// apiUrl vacio en desarrollo: las peticiones van a "/api/..." (mismo origen) y el
// proxy de Angular (proxy.conf.json) las redirige a http://localhost:3000.
// En produccion, poner aqui la URL publica del backend.
export const environment = {
  apiUrl: '',
};
